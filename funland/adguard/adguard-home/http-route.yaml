apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: adguard
spec:
  parentRefs:
    - name: internal
      namespace: gateway
      sectionName: https
  hostnames:
    - "adguard.batlab.io"
  rules:
    - backendRefs:
        - name: adguard-setup
          port: 3000