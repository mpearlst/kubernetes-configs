apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: privatebin
spec:
  parentRefs:
    - name: internal
      namespace: gateway
      sectionName: https
  hostnames:
    - "paste.batlab.io"
  rules:
    - backendRefs:
        - name: privatebin
          port: 8080